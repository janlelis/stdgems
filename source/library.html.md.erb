## Description

<%= gem_description(gem_info, gem_type) %>

## Info

<%= gem_details_properties(gem_info, gem_type) %>

## Gem Sources

<%= gem_sources(gem_info, gem_type) %>

<% if gem_notes(gem_info, gem_type) %>
## Notes

<%= gem_notes(gem_info, gem_type) %>
<% end %>

<%= partial 'footer' %>
